# K1a injections — Claude half (C1, K1-1, K1-2)

Authorship split (K1a pre-reg section 1): Claude wrote these fixtures and
properties and writes NO code for C1, K1-1, or K1-2. Hermes writes that code
and must not read these directories until the K1a-RUN seal is published.

Every directory contains: 10 positives, 3 hard negatives, properties.py,
index.md, MEASUREMENTS.txt. Measurements were generated by running the
relevant mechanical rule on each fixture; the expected columns were copied
from the measurements, not asserted. Every properties.py was executed for
its MIN_EXAMPLES before commit; the C1 property was additionally falsified
against the sealed rounding rule (1000 examples, 0 violations) after a first
draft failed at 74/200 and exposed two section-2 ambiguities, filed in
injections-k1a/c1/index.md.

Placement: extract at repo root. Commit unchanged. Do not edit. Fixture
defects found later are section-4 adjudications, filed, not fixed.
