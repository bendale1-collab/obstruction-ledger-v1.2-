# Ledger Entry — K1 RED

Filed: 2026-09-11
Type: RUN-VERDICT

## Finding

K1 RED. Run at d00f75f, public rows only. Holdout not revealed and remains
sealed at fd2d6faa9b06d8fc6e108eac70583b889ab6e2424eea0f2b4891fe3c9dc8c8e1

## Three independent causes, adjudicated per §4

### ENVIRONMENT — K1-1, K1-2

Python 3.9.6; both use PEP 604 `X | None` (3.10+). Failed at import.
40 fixtures and 4 retrodiction rows produced no result. Not a code defect
and not an injection defect: the sealed spec stated no interpreter version,
and both code agents were instructed not to run their code, so no smoke
test existed. Workflow defect, Claude-attributed.

### HARNESS — K1-3..K1-6

Agent B's code is present at HEAD (2042bf2, ancestor, tracked, 958 lines).
work/k1-test.py hardcodes three scripts (lines 165–192) with no discovery
loop and no evaluators for the other four. Harness authored by Agent A
under a scope that excluded knowledge of B's work. Harness defect,
workflow-attributed.

### INJECTION — C1

Every Claude-authored C1 fixture opens "# packet <case>"; the prose-number
regex reads the "-01" of the case label as the value -1 and compares it
to every table value. 9 of 10 negatives fire on this. C1's rounding logic
is demonstrably working (neg-04 third finding computes 0.985 vs 0.970
correctly). C1 recall 9/10; FPR unmeasured. Injection defect,
Claude-attributed.

## Per §5

Nothing is repaired in place. K1 stands RED. A successor pre-registration
(K1a) must be written, hashed, and published before any rerun, stating
in advance: the interpreter version, a harness covering all seven checks,
and C1 fixtures carrying no numerals in fixture headers.

Filed 2026-09-11. No edits to code, fixtures, or spec.
