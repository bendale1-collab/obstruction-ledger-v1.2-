# Extractor fixtures — provenance and licence

## flask_pr_6095.*

Real, unmodified `git` output captured 2026-09-13 from `pallets/flask`
commit `548994ea0c61465574cbfdd636a894a692f54b94` ("Replace the use of
private `monkeypatch` fixture API (#6095)"), against its first parent.

- `flask_pr_6095.name_status.txt` — `git diff --name-status <merge>^1..<merge>`
- `flask_pr_6095.diff` — `git diff <merge>^1..<merge>`
- `flask_pr_6095.message.txt` — `git log -1 --format=%B <merge>`

Licence: BSD-3-Clause, Copyright 2010 Pallets. Redistribution in source
form is permitted provided the copyright notice, the list of conditions
and the disclaimer are retained; the upstream `LICENSE.txt` carries them
and is reproduced at https://github.com/pallets/flask/blob/main/LICENSE.txt.
Redistribution here is therefore permitted and this sample is real.

Note: the clone under `~/ol/repo/data/flask` is `--depth 1` and holds a
single commit, so it carries no PR history. This sample was taken from a
separate deeper clone of the same public repository.

## terminal_wrench_*

See the per-file note below; recorded when that extractor's fixture landed.
