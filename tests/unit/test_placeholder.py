"""Reserved for ol/ unit tests (pipeline stage 2, agent-runbook.md §17a).

Author-A/author-B territory once ol/checks/ exists; ol/audit/ unit tests
from B1-01 land here too. Empty save for this placeholder until then, so
mutmut (P0-16) has a real, importable test-selection target.
"""

from __future__ import annotations


def test_placeholder() -> None:
    assert True
